# Test dependencies — install alongside requirements.txt for local development
# and CI. Never needed in the production Docker image.
#
# Install:
#   pip install -r requirements.txt -r requirements-test.txt
#
pytest==9.0.3
pytest-asyncio==1.3.0